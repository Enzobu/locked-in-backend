framework:
    rate_limiter:
        # Public account registration, throttled per client IP.
        registration:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
